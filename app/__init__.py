"""
Enterprise AI Operations Copilot — top-level package.

Layers:
  app.core       — config, logging, exceptions
  app.llm        — LLM client and embeddings factories
  app.knowledge  — document loading, splitting, vector store, RAG
  app.tools      — LangChain tools and central registry
  app.agent      — agent orchestration
  app.ui         — Streamlit chat interface
"""
